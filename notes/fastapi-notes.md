# fastapi notes - learnings

- The `uvx fastapi-new` command is a tool for quickly creating a basic FastAPI project structure using the high-performance uv Python package manager.

## Create a new project in the current directory

If you want to set up a FastAPI project within your current working directory, run the command without a project name:

```bash
uvx fastapi-new
```
